<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>PCPs</title>
    <style>
        .tab {
            padding: 10px;
            margin: 5px;
            background-color: #f2f2f2;
            border: 1px solid #ddd;
            cursor: pointer;
            text-decoration: none;
            color: black;
        }

        .tab:hover {
            background-color: #ddd;
        }
    </style>
</head>
<body>
<div class="tabs">
        <a href="/" class="tab">Home</a> 
        <a href="/patients" class="tab">Patients</a>
        <a href="/pcps" class="tab">PCPs</a>
        <a href="/visits" class="tab">Visits</a>
        <a href="/locations" class="tab">Locations</a>
    </div>
    <h1>Primary Care Providers</h1>
    
    <h2>Add New PCP</h2>
    <form method="POST">
    <legend><strong>Add PCP</strong></legend>
        <fieldset class="fields">
        <label for="first_name">First Name:</label>
        <input type="text" id="first_name" name="first_name">
        <br>
        <label for="last_name">Last Name:</label>
        <input type="text" id="last_name" name="last_name">
        <br>
        <label for="pcp_specialty">Specialty:</label>
        <input type="text" id="pcp_specialty" name="pcp_specialty">
        <br>
        <label for="locations">Locations: </label>
            <select id="locations" name="locations">
              <option value="location1">Location 1</option>
              <option value="location2">Location 2</option>
              <option value="location3">Location 3</option>
          </select>
        </fieldset>
        <input type="submit" value="Add PCP">
    </form>

    <h2>List of PCPs</h2>
<table border="3" cellpadding="5">
<thead>
<tr>
    <!-- Iterate through each key in the first entry to get the
    column name -->
    {% for key in pcps[0].keys() %}

    <!-- Create a <th> tag with the key inside of it, this will be
    our header row -->
    <th>{{ key }}</th>

    <!-- End of this for loop -->
    {% endfor %}
    <th> EDIT </th>
    <th> DELETE </th>
</tr>
<tbody>

<!-- Now, iterate through every person in pcps -->
{% for person in pcps %}
<tr>
    <!-- Then iterate through every key in the current person dictionary -->
    {% for key in person.keys() %}

    <!-- Create a <td> element with the value of that key in it -->
    <td>{{person[key]}}</td>
    {% endfor %}
    <td><a href="/pcps/{{person.pcp_id}}" class="btn btn-default">EDIT</a></td>
    <td><a href="/delete_pcp/{{person.pcp_id}}" class="btn btn-default">DELETE</a></td>
</tr>
{% endfor %}
</table>
</body>
</html>